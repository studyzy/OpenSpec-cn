/**
 * Skill Template Workflow Modules
 *
 * This file is generated by splitting the legacy monolithic
 * templates file into workflow-focused modules.
 */
import type { SkillTemplate, CommandTemplate } from '../types.js';

export function getVerifyChangeSkillTemplate(): SkillTemplate {
  return {
    name: 'openspec-verify-change',
    description: '验证实现是否与变更产出物匹配。当用户想在归档前验证实现是否完整、正确且一致时使用。',
    instructions: `验证实现是否与变更产出物（规范、任务、设计）匹配。

**输入**：可选指定变更名称。如果省略，检查是否可以从对话上下文推断。如果模糊或不明确，你必须提示用户选择可用变更。

**步骤**

1. **如果未提供变更名称，提示用户选择**

   运行 \`openspec-cn list --json\` 获取可用变更。使用 **AskUserQuestion tool** 让用户选择。

   显示有实现任务的变更（tasks 产出物存在）。
   如果可用，显示每个变更使用的 Schema。
   将有未完成任务的变更标记为"（进行中）"。

   **重要**：不要猜测或自动选择变更。始终让用户选择。

2. **检查状态以了解 Schema**
   \`\`\`bash
   openspec-cn status --change "<name>" --json
   \`\`\`
   解析 JSON 以了解：
   - \`schemaName\`：使用的工作流（如 "spec-driven"）
   - \`planningHome\`、\`changeRoot\`、\`artifactPaths\` 和 \`actionContext\`：路径和范围上下文
   - 此变更存在哪些产出物

   如果状态报告 \`actionContext.mode: "workspace-planning"\`，说明完整的工作区实现验证在当前版本中不支持并停止。不要推断仓库本地的实现归属或编辑链接的仓库。

3. **获取规划上下文并加载产出物**

   \`\`\`bash
   openspec-cn instructions apply --change "<name>" --json
   \`\`\`

   这将返回变更目录和 \`contextFiles\`（产出物 ID -> 具体文件路径数组）。从 \`contextFiles\` 读取所有可用产出物。

4. **初始化验证报告结构**

   创建包含三个维度的报告结构：
   - **完整性**：跟踪任务和规范覆盖
   - **正确性**：跟踪需求实现和场景覆盖
   - **一致性**：跟踪设计遵循和模式一致性

   每个维度可以有严重（CRITICAL）、警告（WARNING）或建议（SUGGESTION）问题。

5. **验证完整性**

   **任务完成度**：
   - 如果 \`contextFiles.tasks\` 存在，读取其中每个文件路径
   - 解析复选框：\`- [ ]\`（未完成）vs \`- [x]\`（已完成）
   - 统计已完成与总任务数
   - 如果存在未完成任务：
     - 为每个未完成任务添加严重问题
     - 建议："完成任务：<描述>" 或 "如果已实现则标记为完成"

   **规范覆盖**：
   - 如果 \`contextFiles.specs\` 中存在增量规范：
     - 提取所有需求（标记为 "### Requirement:"）
     - 对每个需求：
       - 在代码库中搜索与需求相关的关键词
       - 评估实现是否可能存在
     - 如果需求看起来未实现：
       - 添加严重问题："未找到需求实现：<需求名称>"
       - 建议："实现需求 X：<描述>"

6. **验证正确性**

   **需求实现映射**：
   - 对增量规范中的每个需求：
     - 搜索代码库寻找实现证据
     - 如果找到，记录文件路径和行范围
     - 评估实现是否匹配需求意图
     - 如果检测到偏差：
       - 添加警告："实现可能偏离规范：<详情>"
       - 建议："对照需求 X 审查 <file>:<lines>"

   **场景覆盖**：
   - 对增量规范中的每个场景（标记为 "#### Scenario:"）：
     - 检查代码中是否处理了条件
     - 检查是否存在覆盖该场景的测试
     - 如果场景看起来未覆盖：
       - 添加警告："场景未覆盖：<场景名称>"
       - 建议："为场景添加测试或实现：<描述>"

7. **验证一致性**

   **设计遵循**：
   - 如果 \`contextFiles.design\` 存在：
     - 提取关键决策（查找 "Decision:"、"Approach:"、"Architecture:" 等章节）
     - 验证实现是否遵循这些决策
     - 如果检测到矛盾：
       - 添加警告："未遵循设计决策：<决策>"
       - 建议："更新实现或修改 design.md 以匹配实际情况"
   - 如果没有 design.md：跳过设计遵循检查，注明"没有 design.md 可供验证"

   **代码模式一致性**：
   - 审查新代码与项目模式的一致性
   - 检查文件命名、目录结构、编码风格
   - 如果发现显著偏差：
     - 添加建议："代码模式偏差：<详情>"
     - 建议："考虑遵循项目模式：<示例>"

8. **生成验证报告**

   **摘要记分卡**：
   \`\`\`
   ## 验证报告：<change-name>

   ### 摘要
   | 维度       | 状态               |
   |------------|-------------------|
   | 完整性     | X/Y 任务，N 个需求 |
   | 正确性     | M/N 需求已覆盖    |
   | 一致性     | 已遵循/有问题     |
   \`\`\`

   **按优先级分组的问题**：

   1. **严重**（归档前必须修复）：
      - 未完成的任务
      - 缺失的需求实现
      - 每个都有具体、可操作的建议

   2. **警告**（应该修复）：
      - 规范/设计偏差
      - 缺失的场景覆盖
      - 每个都有具体的建议

   3. **建议**（最好修复）：
      - 模式不一致
      - 小改进
      - 每个都有具体的建议

   **最终评估**：
   - 如果有严重问题："发现 X 个严重问题。归档前请修复。"
   - 如果只有警告："无严重问题。有 Y 个警告需要考虑。可以归档（附带改进建议）。"
   - 如果全部通过："所有检查已通过。可以归档。"

**验证启发式**

- **完整性**：关注客观的清单项（复选框、需求列表）
- **正确性**：使用关键词搜索、文件路径分析、合理推断 - 不要求完美确定性
- **一致性**：查找明显的不一致，不要吹毛求疵
- **误报**：不确定时，优先使用建议而非警告，警告而非严重
- **可操作性**：每个问题必须有具体的建议，适用时包含文件/行引用

**优雅降级**

- 如果只有 tasks.md 存在：仅验证任务完成度，跳过规范/设计检查
- 如果有任务 + 规范：验证完整性和正确性，跳过设计
- 如果有完整产出物：验证所有三个维度
- 始终注明跳过了哪些检查以及原因

**输出格式**

使用清晰的 markdown：
- 用表格展示摘要记分卡
- 用分组列表展示问题（严重/警告/建议）
- 代码引用格式：\`file.ts:123\`
- 具体、可操作的建议
- 不要使用模糊的建议如"考虑审查"`,
    license: 'MIT',
    compatibility: 'Requires openspec CLI.',
    metadata: { author: 'openspec', version: '1.0' },
  };
}

export function getOpsxVerifyCommandTemplate(): CommandTemplate {
  return {
    name: 'OPSX: Verify',
    description: '验证实现是否与变更产出物匹配',
    category: 'Workflow',
    tags: ['workflow', 'verify', 'experimental'],
    content: `验证实现是否与变更产出物（规范、任务、设计）匹配。

**输入**：可选在 \`/opsx:verify\` 后指定变更名称（如 \`/opsx:verify add-auth\`）。如果省略，检查是否可以从对话上下文推断。如果模糊或不明确，你必须提示用户选择可用变更。

**步骤**

1. **如果未提供变更名称，提示用户选择**

   运行 \`openspec-cn list --json\` 获取可用变更。使用 **AskUserQuestion tool** 让用户选择。

   显示有实现任务的变更（tasks 产出物存在）。
   如果可用，显示每个变更使用的 Schema。
   将有未完成任务的变更标记为"（进行中）"。

   **重要**：不要猜测或自动选择变更。始终让用户选择。

2. **检查状态以了解 Schema**
   \`\`\`bash
   openspec-cn status --change "<name>" --json
   \`\`\`
   解析 JSON 以了解：
   - \`schemaName\`：使用的工作流（如 "spec-driven"）
   - \`planningHome\`、\`changeRoot\`、\`artifactPaths\` 和 \`actionContext\`：路径和范围上下文
   - 此变更存在哪些产出物

   如果状态报告 \`actionContext.mode: "workspace-planning"\`，说明完整的工作区实现验证在当前版本中不支持并停止。不要推断仓库本地的实现归属或编辑链接的仓库。

3. **获取规划上下文并加载产出物**

   \`\`\`bash
   openspec-cn instructions apply --change "<name>" --json
   \`\`\`

   这将返回变更目录和 \`contextFiles\`（产出物 ID -> 具体文件路径数组）。从 \`contextFiles\` 读取所有可用产出物。

4. **初始化验证报告结构**

   创建包含三个维度的报告结构：
   - **完整性**：跟踪任务和规范覆盖
   - **正确性**：跟踪需求实现和场景覆盖
   - **一致性**：跟踪设计遵循和模式一致性

   每个维度可以有严重（CRITICAL）、警告（WARNING）或建议（SUGGESTION）问题。

5. **验证完整性**

   **任务完成度**：
   - 如果 \`contextFiles.tasks\` 存在，读取其中每个文件路径
   - 解析复选框：\`- [ ]\`（未完成）vs \`- [x]\`（已完成）
   - 统计已完成与总任务数
   - 如果存在未完成任务：
     - 为每个未完成任务添加严重问题
     - 建议："完成任务：<描述>" 或 "如果已实现则标记为完成"

   **规范覆盖**：
   - 如果 \`contextFiles.specs\` 中存在增量规范：
     - 提取所有需求（标记为 "### Requirement:"）
     - 对每个需求：
       - 在代码库中搜索与需求相关的关键词
       - 评估实现是否可能存在
     - 如果需求看起来未实现：
       - 添加严重问题："未找到需求实现：<需求名称>"
       - 建议："实现需求 X：<描述>"

6. **验证正确性**

   **需求实现映射**：
   - 对增量规范中的每个需求：
     - 搜索代码库寻找实现证据
     - 如果找到，记录文件路径和行范围
     - 评估实现是否匹配需求意图
     - 如果检测到偏差：
       - 添加警告："实现可能偏离规范：<详情>"
       - 建议："对照需求 X 审查 <file>:<lines>"

   **场景覆盖**：
   - 对增量规范中的每个场景（标记为 "#### Scenario:"）：
     - 检查代码中是否处理了条件
     - 检查是否存在覆盖该场景的测试
     - 如果场景看起来未覆盖：
       - 添加警告："场景未覆盖：<场景名称>"
       - 建议："为场景添加测试或实现：<描述>"

7. **验证一致性**

   **设计遵循**：
   - 如果 \`contextFiles.design\` 存在：
     - 提取关键决策（查找 "Decision:"、"Approach:"、"Architecture:" 等章节）
     - 验证实现是否遵循这些决策
     - 如果检测到矛盾：
       - 添加警告："未遵循设计决策：<决策>"
       - 建议："更新实现或修改 design.md 以匹配实际情况"
   - 如果没有 design.md：跳过设计遵循检查，注明"没有 design.md 可供验证"

   **代码模式一致性**：
   - 审查新代码与项目模式的一致性
   - 检查文件命名、目录结构、编码风格
   - 如果发现显著偏差：
     - 添加建议："代码模式偏差：<详情>"
     - 建议："考虑遵循项目模式：<示例>"

8. **生成验证报告**

   **摘要记分卡**：
   \`\`\`
   ## 验证报告：<change-name>

   ### 摘要
   | 维度       | 状态               |
   |------------|-------------------|
   | 完整性     | X/Y 任务，N 个需求 |
   | 正确性     | M/N 需求已覆盖    |
   | 一致性     | 已遵循/有问题     |
   \`\`\`

   **按优先级分组的问题**：

   1. **严重**（归档前必须修复）：
      - 未完成的任务
      - 缺失的需求实现
      - 每个都有具体、可操作的建议

   2. **警告**（应该修复）：
      - 规范/设计偏差
      - 缺失的场景覆盖
      - 每个都有具体的建议

   3. **建议**（最好修复）：
      - 模式不一致
      - 小改进
      - 每个都有具体的建议

   **最终评估**：
   - 如果有严重问题："发现 X 个严重问题。归档前请修复。"
   - 如果只有警告："无严重问题。有 Y 个警告需要考虑。可以归档（附带改进建议）。"
   - 如果全部通过："所有检查已通过。可以归档。"

**验证启发式**

- **完整性**：关注客观的清单项（复选框、需求列表）
- **正确性**：使用关键词搜索、文件路径分析、合理推断 - 不要求完美确定性
- **一致性**：查找明显的不一致，不要吹毛求疵
- **误报**：不确定时，优先使用建议而非警告，警告而非严重
- **可操作性**：每个问题必须有具体的建议，适用时包含文件/行引用

**优雅降级**

- 如果只有 tasks.md 存在：仅验证任务完成度，跳过规范/设计检查
- 如果有任务 + 规范：验证完整性和正确性，跳过设计
- 如果有完整产出物：验证所有三个维度
- 始终注明跳过了哪些检查以及原因

**输出格式**

使用清晰的 markdown：
- 用表格展示摘要记分卡
- 用分组列表展示问题（严重/警告/建议）
- 代码引用格式：\`file.ts:123\`
- 具体、可操作的建议
- 不要使用模糊的建议如"考虑审查"`
  };
}
